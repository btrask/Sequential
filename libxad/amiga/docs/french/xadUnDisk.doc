NOM
        xadUnDisk 1.16

SYNTAXE
        xadUnDisk FROM/A,TO,LOWCYL/N,HIGHCYL/N,ENTRY/N,PASSWORD,
        SAVETEXTS/K,NE=NOEXTERN/S,INFO=LIST/S,SHOWTEXTS/S,OW=OVERWRITE/S,
        IG=IGNOREGEOMETRY/S,FORMAT/S,DIMG=DISKIMAGE/S,NAI=NOASKINSERT/S,
        USL=USESECTORLABELS

DESCRIPTION
        xadUnDisk est l'outil d'extraction des données des archives disques.

        FROM spécifie le disque (disquette) qui doit être traité. Les motifs
        sont maintenant gérés.

        TO est la destination. Ce peut être un nom de périphérique comme
        RAD: ou n'importe quel nom de fichier. TO est obligatoire lorsque
        l'option LIST n'est pas utilisée. Quand l'extraction se fait vers
        un fichier, c'est une image du disque (souvent appelé ADF), qui
        sera utilisée avec toutes les caractéristiques de l'ADF.

        LOWCYL et HIGHCYL indiquent le premier et le dernier cylindre de la
        portion à désarchiver. Certains clients ne gèrent pas les numéros de
        cylindre et ignoreront ces options.

        ENTRY spécifie l'entrée qui doit être désarchivée. Les disques
        normaux n'utilisent qu'une seule entrée, il n'est par conséquent pas
        nécessaire d'utiliser cette option dans ce cas. Mais vous aurez
        peut-être besoin de cette option si vous utilisez des archives
        réparties sur plusieurs disques, vous pourrez ainsi spécifier la
        bonne entrée. La première entrée est : 1.

        PASSWORD peut-être utilisé pour passer le mot de passe au programme
        de désarchivage.

        Après le mot SAVETEXTS vous pouvez préciser un nom de fichier qui
        sera utiliser pour sauver des informations lors du traitement.
        Il est automatiquement attribué un suffixe à ce nom de fichier, il
        s'agit d'un numéro. Exemple : "SAVETEXTS texte" produira les fichiers
        suivants : texte.1, texte.2, ...

        NOEXTERN interdit l'utilisation des clients externes lors du
        processus de reconnaissance.
        
        INFO ou LIST affichent uniquement les informations sur les archives.
        Il n'y a pas de décompression.
        
        SHOWTEXTS indique au programme d'afficher les textes d'informations,
        si il y en a au sein de l'archive.
        
        OVERWRITE force le programme à remplacer le fichier de destination
        si il existe déjà.

        IGNOREGEOMETRY force le désarchivage même si le format du périphérique
        ne convient pas ou ne correspond pas à l'original.

        Indiquez USESECTORLABELS lorsque vous voulez sauver sur disque
        les « sectorLabels » (pas d'effet sur la sortie des fichiers).
        Tous les archiveurs ne gère pas cet élément et tous les
        périphériques de destination ne permettent pas cette sauvegarde.
        La plupart du temps vous n'aurez pas besoin de cette option, car
        les labels ne contiennent que très rarement des informations
        utiles.

        Si le disque de destination n'est pas formatée, utilisez FORMAT
        pour que cela ce fasse en même temps que l'écriture.

        Indiquez DISKIMAGE si vous voulez écrire une image disque sur une
        disquette. Le programme vous demandera aussi si vous voulez traiter
        un fichier en tant qu'image si le type de données n'est pas détecté
        à l'aide du mot clé DIMG.

        Ceci peut également être utilisé pour lire une image-disque à partir
        d'un périphérique ou pour copier deux disques.

        Indiquez NOASKINSERT si vous ne voulez pas de requête d'insertion
        de disquette.

EXEMPLES
        Shell> xadUnDisk archive.dms RAD:
                Désarchive le fichier DMS dans RAD:

        Shell> xadUnDisk archive.dms LIST
                Affiche les informations sur le fichier archive dms.

HISTORIQUE
        xadUnDisk 1.16
        - Les nombres sont maintenant affichés en mode non-signé.

        xadUnDisk 1.15
        - Un petit changement visuel.

        xadUnDisk 1.14
        - Ajout du support pour la gestion des périphériques comme l'entrée
          des images de diques (passer l'entrée <I> de dialogue etc) et
          des motifs.

        xadUnDisk 1.13
        - Correction dans la gestion des entrées disque et des mots clés
          DIMG.

        xadUnDisk 1.12
        - Supporte la lecture à partir d'un périphérique comme DF0: pour
          produire des fichiers images.

        xadUnDisk 1.11
        - Retour Enforcer corrigé.
        - Ajout de l'option USESECTORLABELS.

        xadUnDisk 1.10
        - Les fichiers images passent maintenant par la bibliothèque
          « xadmaster.library ».

        xadUnDisk 1.9
        - Ajout de l'option NOASKINSERT.

        xadUnDisk 1.8
        - Correction d'une erreur.

        xadUnDisk 1.7
        - Correction d'une erreur.

        xadUnDisk 1.6
        - Ajout du mot clé FORMAT et du support des images disques.

        xadUnDisk 1.5
        - Avant l'écriture sur disque, il vous sera demandé de l'insérer ou
          d'annuler l'opération.

        xadUnDisk 1.4
        - Correction du tampon dans (hook).

        xadUnDisk 1.3
        - Correction d'un bug. (hook).

        xadUnDisk 1.2
        - Ajout de la gestion des fichiers corrompus.

        xadUnDisk 1.1
        - Ajout de la gestion de la description des fichiers.

        xadUnDisk 1.0
        - 1 ère diffusion.

COPYRIGHT
        xadUnDisk est libre pour toute utilisation non commerciale.

AUTEUR
        Dirk Stöcker
        Geschwister-Scholl-Straße 10
        01877 Bischofswerda
        Allemagne
        
        Dirk Stoecker <soft@dstoecker.de>
