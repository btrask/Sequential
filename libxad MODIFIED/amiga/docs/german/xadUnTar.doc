NAME
	xadUnTar 1.7

ÜBERSICHT
	xadUnTar FROM/A,DEST=DESTDIR,PASSWORD/K,FILE/M,NAMESIZE/K/N,
	FFS=OFS/S,SFS/S,INFO=LIST/S,Q=QUIET/S,AM=ASKMAKEDIR/S,
	OW=OVERWRITE/S,NA=NOABS/S,ND=NODATE/S,NE=NOEXTERN/S,
	NKP=NOKILLPART/S,NP=NOPROT/S,NT=NOTREE/S,SHORTNAME/S

BESCHREIBUNG
	Das Programm xadUnTar ist ein Programm zum Extrahieren von Daten
	aus Tar-Archiven und komprimierten Tar-Archiven. Das schließt die
	Typen .tar.bz, .tar.bz2, .tar.gz, .tar.Z, .tar und andere ein.

	FROM gibt den Namen des zu behandelnden Dateiarchives an.
	DESTDIR gibt das Zielverzeichnis an. Das Verzeichnis wird erstellt,
	wenn es nicht existiert (außer ASMAKEDIR ist angeschaltet und Sie
	wählen Nein). Es gibt eine gefährliche Archive, welche absolute
	Pfadnamen (z.B. mit LIBS: oder RAM: beginnend) nutzen. xadUnTar
	extrahiert diese Dateien an ihre absolute Position (außer NOTREE
	oder NOABS wird genutzt).

	Das Argument PASSWORD kann benutzt werden, um ein nötiges Paßwort
	zum Extrahieren von verschlüsselten Archiven zu übergeben. Die
	Angabe des Schlüsselwortes PASSWORD wird vorrausgesetzt.

	Der Parameter FILE erlaubt es Dateinamen (mit Jokerzeichen)
	anzugeben, welche extrahiert werden sollen. Wird FILE nicht
	angegeben, so werden alle Dateien extrahiert. Bei Angabe von FILE
	werden nur die Dateien extrahiert, die mit dem angegebenem Such-
	muster übereinstimmen.
	Notiz: Wenn das NOTREE-Schlüsselwort nicht benutzt wird, so muß das
	Suchmuster auch die Verzeichnisstruktur berücksichtigen.

	Der Parameter NAMESIZE erlaubt die Angabe der maximalen Länge eines
	Dateinamen. Das ist nützlich, weil Archive, welche auf Unix-Rechnern
	erstellt wurden, teilweise länge Dateinamen haben, als auf dem Amiga
	möglich ist. Sobald ein Name länger ist die angegeben Zahl ist,
	werden sie gebeten einen neuen Namen einzugeben. Verzeichnisnamen
	werden einfach ohne Nachfrage abgeschnitten.
	Die Paramter FFS oder OFS ersetzen den Parameter NAMESIZE and geben
	eine Standardlänge von 30 Zeichen vor, welches die Länge des Amiga-
	Standard-Filesystem ist. Der Paramter SFS gibt eine Länge von 100
	Zeichen vor, die Standardlänge des SmartFilesystem.

	Das Argument NOEXTERN verhindert die Nutzung der externen
	Clients zur Erkennung der Archiverprogramme.
	
	INFO oder LIST zeigen Informationen über eine Archivdatei und
	extrahieren keinerlei Daten.

	OVERWRITE bewirkt, daß eine möglicherweise existierende Zieldatei
	ohne Nachfrage überschrieben wird. Normalerweise wird nachgefragt.
	
	NOTREE überspringt alle Verzeichnisnamen und schreibt die Dateien
	direkt ins Zielverzeichnis.

	SHORTNAME reduziert die Anzeige des Namens bei INFO und beim
	Extrahieren auf den eigentlichen Dateinamen.
	
	ASKMAKEDIR bewirkt, daß das Programm vorm Erstellen eines
	Verzeichnisses nachfragt.

	NOPROT verhindert das Setzen der Schutzbits und NODATE verhindert
	das Setzen des Datums.
	
	NOABS verhindert die Erzeugung absoluter Pfadnamen.

	Wenn Sie den Paramter NOKILLPART angeben, werden unvollständige oder
	defekte Ausgabedateien nicht mehr gelöscht.

	Die QUIET-Option schaltet die Fortschrittsanzeige aus. Sie schaltet
	allerdings auch die Fragen zum Überspringen, Umbennen oder Über-
	schreiben von Dateien aus.

BEISPIELE
	Shell> xadUnTar archive.tar RAM:
		Extrahiert die Daten des tar-Archives nach RAM:.

	Shell> xadUnTar archive.tar.gz RAM: #?.txt #?.doc Contents
		Extrahiert alle .txt- und .doc-Dateien and die Datei
		"Contents" des Tar-Gzip-Archives nach RAM:

	Shell> xadUnTar archive.tar.bz LIST
		Zeigt Informationen über die Datei archive.tar.bz an.

GESCHICHTE
	xadUnTar 1.6
	- Unterstützung für Pipes und Devices eingebaut

	xadUnTar 1.6
	- Unterstützung für einen zweiten Typ langer Namen eingeführt.

	xadUnTar 1.5
	- Die Anzeige von Links funktioniert nun richtig.

	xadUnTar 1.4
	- Fehlerkorrekturen
	- Unterstützung für Namensverlängerungsblöcke eingeführt.
	- SHORTNAME-Option eingeführt.

	xadUnTar 1.3
	- Reine Tar-Archive werden schneller bearbeitet.

	xadUnTar 1.2
	- extrahiert jetzt auch leere Dateien korrekt

	xadUnTar 1.1
	- kleine Fehlerkorrektur

	xadUnTar 1.0
	- Erste Veröffentlichung

COPYRIGHT
	xadUnTar ist bei nichtkommerzielle Nutzung frei vertreibbar.

AUTOR
	Dirk Stöcker
	Geschwister-Scholl-Straße 10
	01877 Bischofswerda
	GERMANY
	
	Dirk Stoecker <soft@dstoecker.de>
